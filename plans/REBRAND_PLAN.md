# Rebrand plan v3 — "Reactive UI Toolkit": FULL conversion + org migration (Godot repo leg)

**Status: PLAN — Phase-0 has TWO remaining micro-decisions (Q1/Q2 below); everything else is
owner-ratified (2026-07-27).** Scope per owner: **complete rename — nothing stays** — brand
surfaces AND identifiers (folders, class prefix) convert; clean break with a codemod (no
compat window); Godot repo only in this wave (Unity/Unreal sibling plans follow); the C# leg
stays on hold until after.

**Sequencing (ratified):** org transfer FIRST, in-repo rename SECOND — one URL sweep straight
to final org values; transfer is content-free; CI/secrets verified on the org before the wave
lands; redirects cover the gap. The extension update ships WITH the wave's Publish (its
bundled paths reference the renamed folders, so it cannot precede them).

**Key de-risking fact (verified 2026-07-27):** the family-core corpus hash
(`scripts/corpus-hash.mjs` over the `_tiers.familyCore` sections of
`guitkx-scanner-cases.json`, engine-normalized `GUITKX→TKX`) contains **zero** `RUIVNode`
occurrences → **the class rename needs NO family corpus re-pin and NO Unity/Unreal
coordination.** The 88 `RUIVNode` contract occurrences are Godot-LOCAL fixtures/goldens,
regenerated by one `contract_dump` run (it writes goldens when run without `--check`).

**EXECUTOR CONTRACT (read first):**
- Every step names exact file(s), exact OLD string, exact NEW string, and a verification
  command. Execute in order. If an OLD string is not found EXACTLY: **STOP and report** — no
  improvisation, no fuzzy matching.
- String replacements are per the instruction's scope (per-file, or repo-wide WITH the stated
  exclusions) — never invent your own sweep.
- Class-name replacement is **whole-word** (`\bOldName\b`) — stated per group.
- After each group: run that group's verification. After Phase 3: the FULL battery (§6.J).
- Push ONLY to the feature branch; owner PRs (house flow). Steps marked **[OWNER]** are
  console/browser actions — list them, never attempt them.

---

## 1. Name Registry (ratified + the two open micro-decisions)

| # | Context | OLD (exact) | NEW (exact) |
|---|---|---|---|
| N1 | Umbrella brand | — | `Reactive UI Toolkit` |
| N2 | GitHub org | `yanivkalfa` | `reactive-ui-toolkit` |
| N3 | Repo name | `ReactiveUI-Godot` | **TBD-R1** (rec: `godot`) |
| N4 | Repo URL root | `https://github.com/yanivkalfa/ReactiveUI-Godot` | `https://github.com/reactive-ui-toolkit/<N3>` |
| N5 | Library display name | `Reactive UI` / `ReactiveUI for Godot` / `Reactive UI (React for Godot)` | **TBD-R2** (rec: `Reactive UI Toolkit — Godot`) |
| N6 | Editor addon display name | `Reactive UI Editor` | rec: `Reactive UI Toolkit — Godot Editor` |
| N7 | License product name | `ReactiveUI for Godot` | rec: `Reactive UI Toolkit — Godot` |
| N8 | License credit line | `Made with ReactiveUI` | **TBD-R4** (rec: keep) |
| N9/N10 | Extension display names + IDs | `GUITKX (Godot - VS Code)` / `GUITKX (Godot - VS2022)`, publisher `ReactiveUITK`, ids `guitkx` / `GuitkxVsix.ReactiveUITK` | **RESOLVED (owner, 2026-07-27): ALL UNCHANGED** — only content underneath (descriptions, READMEs, bundled paths, URLs) updates |
| N11 | Docs site title | `ReactiveUI for Godot — Documentation` | `<N5> — Documentation` |
| N12 | Pages base path | `/ReactiveUI-Godot/` | `/<N3>/` |
| N13 | Author fields | `ReactiveUIToolKit` | rec: `Reactive UI Toolkit` |
| N14/N15 | AssetLib listing titles | `Reactive UI (React for Godot)` / `Reactive UI Editor` | rec: `<N5>` / `<N6>` |
| **N16** | **Class prefix (RESOLVED)** | `RUI` (33 global classes, table in §6.E) | `Ruitk` — e.g. `RUIVNode` → `RuitkVNode` |
| **N17** | **Runtime addon folder (RESOLVED)** | `addons/reactive_ui` | `addons/reactive_ui_toolkit` |
| **N18** | Editor addon folder | `addons/reactive_ui_editor` | rec: `addons/reactive_ui_toolkit_editor` (symmetry; ratify with R2 batch) |
| **N19** | Analyzer bundle folder | `addons/reactive_ui_analyzer` | **Q1 (open)** — see below |
| **N20** | Release asset zips | `reactive_ui-<ver>.zip` / `reactive_ui_editor-<ver>.zip` | `reactive_ui_toolkit-<ver>.zip` / `reactive_ui_toolkit_editor-<ver>.zip` |
| N21 | Wave versions | — | runtime **0.13.0** (BREAKING minor), editor **0.11.0**, extensions **0.13.0** (rec — ratify at execution) |
| N22 | Codemod + guide | — | `dev/migrate_0_13_0.gd` (inside the renamed runtime addon) + `MIGRATION-0.13.md` |

**Q1 — analyzer bundle folder** (`addons/reactive_ui_analyzer`, 32 references): the
gdscript-analyzer REPO moves to the org untouched (R8), but the bundled FOLDER name is staged
by OUR publish.yml (lines ~245–250) and referenced by the editor addon's feature detection,
templates, and `.gitignore` line 44. Options: **(a) rename** to
`addons/reactive_ui_toolkit_analyzer` via an `mv` in the packaging step + sweep the 32 refs
(consistent with "nothing stays"; the analyzer repo itself stays untouched) — REC;
**(b) keep** (32 `reactive_ui` tokens survive as the one exception).
**Q2 — unprefixed API classes:** `ReactiveRoot` / `ReactiveRootNode` (77 refs), `V`, `Hooks`
carry no stale token (`Reactive` IS the umbrella; `V`/`Hooks` are family-parity names).
REC: keep all four. If the owner wants `RuitkRoot`/`RuitkRootNode`: add two rows to the §6.E
table (+77 refs + docs) — same mechanics, no extra risk.

**Frozen even in the full conversion (the residual untouchables):** `.guitkx` file extension
+ `GUITKX####` diagnostic codes + `guitkx*` tool/package names (the LANGUAGE brand — renaming
it would be a separate product decision, not this wave); marketplace publisher/extension IDs
AND display names (owner-resolved: unchanged); `@gdscript-analyzer/core`; the Discord invite;
Tier-3 historical bodies (§3).

## 2. Phase 0 — remaining ratifications **[OWNER]**

R1 (repo name), R2 batch (exact N5/N6/N13/N14/N15/N18 strings), R4 (credit line), Q1, Q2,
N21 (version numbers). Already resolved: R3 (license product label — ships with this wave's
release), R5 (extension identity — keep everything, change underneath), R8 (analyzer repo
moves as-is), R9 (icons deferred), R10 (FULL conversion — this document IS the answer).

## 3. Census (measured 2026-07-27) + frozen remainder

| Surface | Count | Disposition |
|---|---|---|
| `Reactive UI` display strings | 111 × 33 files | §6.B |
| `yanivkalfa` URLs | 46 × 27 files | §6.A |
| `reactive_ui` path/identifier tokens | 612 total = base ~411 + `_editor` 169 + `_analyzer` 32 | §6.D (analyzer per Q1) |
| `RUI*` identifier tokens | **1806 × 302 files**; 33 global classes; `RUIVNode` alone 584 | §6.E |
| `-> RUIVNode` classifier surfaces | compiler 13 (3 files) · editor addon 6 (5) · TS server 22 (6) · grammars+schema 7 (3) · contract 88 (87) · examples 45 (45) · docs 130 (32) | §6.E/F |
| `ReactiveUITK` publisher/author | 24 × 14 files | author-display fields §6.B; publisher IDs frozen |
| vocabulary.json (both copies) | 0 RUI tokens | untouched |
| family-core corpus hash | 0 RUI tokens | **unaffected — no re-pin, no sibling coordination** |
| `project.godot` | line 24: both plugin paths | §6.D.4 |
| `.gitignore` | line 5 comment + line 44 analyzer dir | §6.D rules 4/2 |

**Tier 3 — historical record (frozen except live URLs):** CHANGELOG/Discord ENTRY BODIES
(old entries keep `Reactive UI 0.12.1`, old `res://addons/reactive_ui/dev/migrate_*.gd`
command examples — they document shipped releases), MIGRATION-0.10/0.11 bodies,
`plans/archive/**`. Live URLs inside them DO update (§6.A).

## 4. Phase 1 — org creation **[OWNER]**

Create org `reactive-ui-toolkit` (if the name is taken: STOP — the registry shifts). Owner
account = org owner. Domain strategy (`reactiveuitoolkit.info`, owned by the Unity leg) is a
family-wave decision, not this plan's.

## 5. Phase 2 — repo transfers + rename **[OWNER]**

1. Transfer `yanivkalfa/ReactiveUI-Godot` → the org; rename to **N3**.
2. Transfer `yanivkalfa/gdscript-analyzer` → the org (as-is, no rename — R8).
3. Verify post-transfer: Actions enabled; secrets/variables present (`ASSETLIB_*`,
   marketplace PATs); rulesets intact ("Protect dev + master"); Pages source
   (`documentations` branch) still configured.
4. Executor: `git remote set-url origin https://github.com/reactive-ui-toolkit/<N3>.git` in
   the working copy; `git fetch origin` must succeed. `blackout-backups/` mirrors keep their
   recorded old URLs — archives, do not modify.
5. **Never reuse** the freed names under `yanivkalfa` (severs redirects). GitHub Pages URL
   does NOT redirect — handled by §6.C + §7.

## 6. Phase 3 — the conversion (branch `rebrand/umbrella`; one commit per group, this order)

### Group A — repository URL swap (46 occurrences, 27 files)

Every `https://github.com/yanivkalfa/ReactiveUI-Godot` →
`https://github.com/reactive-ui-toolkit/<N3>`; every
`https://raw.githubusercontent.com/yanivkalfa/ReactiveUI-Godot/…` → org+N3 form; every
`yanivkalfa/gdscript-analyzer` → `reactive-ui-toolkit/gdscript-analyzer` (locate with
`git grep -n "yanivkalfa/gdscript-analyzer"` — publish.yml ~line 244 + any packaging fetch).
File list (v2 §6.A, unchanged — 27 files incl. both `.hb` templates, ISSUE_TEMPLATE config,
publish.yml, both CHANGELOG mirrors via root-edit+`cp`, CLAUDE.md, LICENSE-COMMERCIAL.md,
README.md, 4 docs-site files, addon READMEs+CHANGELOGs, changelog.json, server.ts,
overview-template.md, publishManifest.json `repo`, vsixmanifest Description,
vscode package.json repository+description, readme-template.md, HISTORY_RESET_STATE.md).
Generated files (vscode/vs2022/editor CHANGELOGs + vscode README) are fixed by Group-H
regeneration — do not hand-edit them.
**Verify:** `git grep -c "yanivkalfa"` → 0 (after Group H regen).

### Group B — display names, licenses, listings

v2 steps B1–B19 apply with these ratified deltas:
- **Extension manifests (old B9/B11): `displayName`/`DisplayName`, `name`, `publisher`,
  `Identity Id` ALL UNCHANGED** (owner resolution). Only `description` product-label swaps
  (`(ReactiveUI for Godot)` → `(<N5>)`) + URLs (Group A).
- B12 asset templates: the `"cost"` fix is ALREADY on branch `fix/assetlib-cost-proprietary`
  (merge it before this wave or rebase the wave onto it — verify
  `grep '"cost"' .asset-template*.json.hb` shows `Proprietary` before starting). Titles per
  N14/N15. Folder names inside descriptions now DO change (§6.D Rule 5): `enable reactive_ui
  first, then reactive_ui_editor` → the N17/N18 names; `addons/reactive_ui_analyzer` per Q1.
- B6 LICENSE set (5 byte-identical copies): product label per N7 + credit line per R4.
  **Execute AFTER Group D** (the copies live at renamed paths):
  `cp LICENSE addons/reactive_ui_toolkit/LICENSE && cp LICENSE
  addons/reactive_ui_toolkit_editor/LICENSE && cp LICENSE ide-extensions/vscode/LICENSE &&
  cp LICENSE ide-extensions/visual-studio/GuitkxVsix/LICENSE.txt`.
- B18 gallery label + B13 docs identity + B3/B4/B5 README H1s: per N5/N6 as in v2.

### Group C — Pages base path

`ReactiveUIGodotDocs~/vite.config.ts` (~line 111): `base: '/ReactiveUI-Godot/',` →
`base: '/<N3>/',`.

### Group D — FOLDER RENAMES (order-sensitive; substring-hazard rules)

1. `git mv addons/reactive_ui_editor addons/reactive_ui_toolkit_editor`
2. `git mv addons/reactive_ui addons/reactive_ui_toolkit`
3. Path sweep — repo-wide, ORDERED, excluding Tier-3 bodies + `plans/archive/**`:
   - **Rule 1 (first):** `addons/reactive_ui_editor` → `addons/reactive_ui_toolkit_editor`
     (~169 expected).
   - **Rule 2:** `addons/reactive_ui_analyzer` → per Q1: (a) →
     `addons/reactive_ui_toolkit_analyzer` (~32) AND add the packaging `mv` in publish.yml's
     analyzer-staging step; (b) NO CHANGE — later rules must not touch it.
   - **Rule 3 (after 1–2):** `addons/reactive_ui/` (WITH trailing slash) →
     `addons/reactive_ui_toolkit/` — the slash protects the `_editor`/`_analyzer` forms.
   - **Rule 4 (enumerated stragglers):** `git grep -n "addons/reactive_ui\b"` — fix each
     hit individually: `.gitignore` line-5 comment; prose in templates/READMEs; the
     publish.yml zip lines (asset filenames per N20: line ~140
     `dist/reactive_ui-…zip" addons/reactive_ui` → toolkit forms; line ~280 editor zip);
     CLAUDE.md command examples (current-doc tier, updates); README bullets.
   - **Rule 5 (plugin-name prose):** sentences naming the plugins by folder (`enable
     reactive_ui first, then reactive_ui_editor` in the editor asset template + editor
     plugin.cfg description `addons/reactive_ui` dependency mention) → new names.
4. `project.godot` line 24:
   OLD `enabled=PackedStringArray("res://addons/reactive_ui/plugin.cfg", "res://addons/reactive_ui_editor/plugin.cfg")`
   NEW `enabled=PackedStringArray("res://addons/reactive_ui_toolkit/plugin.cfg", "res://addons/reactive_ui_toolkit_editor/plugin.cfg")`
   (Covered by Rules 1+3 — listed for explicit verification.)
5. Delete stale `.godot/` cache locally before the battery (class cache must rebuild).
**Verify:** `git grep -n "addons/reactive_ui\b"` → hits ONLY in Tier-3 bodies +
`plans/archive/**`; `git grep -c "reactive_ui_toolkit"` ≈ 612 ± prose delta; the §6.J build
sweep is the real path validator (bad `res://` paths fail compile loudly).

### Group E — CLASS-PREFIX RENAME (`RUI` → `Ruitk`, whole-word, 33 classes)

Repo-wide per-class whole-word replace (`\bOldName\b`), EXCLUDING `plans/archive/**` (verify
changelog bodies contain no class names first: `git grep -n "RUIVNode" CHANGELOG.md` — if
hits exist in historical entries, leave those lines). Apply ALL rows:

| OLD → NEW (34 pairs incl. Q2 defaults) |
|---|
| `RUIGuitkxCodegen→RuitkGuitkxCodegen` · `RUIGuitkxFormatter→RuitkGuitkxFormatter` · `RUIGuitkxResolve→RuitkGuitkxResolve` · `RUIGuitkxMigrate→RuitkGuitkxMigrate` · `RUIGuitkxMarkup→RuitkGuitkxMarkup` · `RUIGuitkxJsxScan→RuitkGuitkxJsxScan` · `RUIGuitkxLexer→RuitkGuitkxLexer` · `RUIGuitkxConfig→RuitkGuitkxConfig` · `RUIGuitkxDiag→RuitkGuitkxDiag` · `RUIGuitkx→RuitkGuitkx` |
| `RUIRouterLocation→RuitkRouterLocation` · `RUIRouterPath→RuitkRouterPath` · `RUIRouteMatcher→RuitkRouteMatcher` · `RUIRouteRanker→RuitkRouteRanker` · `RUIRouteMatch→RuitkRouteMatch` · `RUIRouter→RuitkRouter` |
| `RUIStyleSheet→RuitkStyleSheet` · `RUIStyle→RuitkStyle` · `RUISignals→RuitkSignals` · `RUISignal→RuitkSignal` |
| `RUIComponentState→RuitkComponentState` · `RUIEditorSettings→RuitkEditorSettings` · `RUIEditorDeps→RuitkEditorDeps` · `RUIConfig→RuitkConfig` · `RUIContext→RuitkContext` · `RUIDiagnostics→RuitkDiagnostics` · `RUIFiber→RuitkFiber` · `RUIHistory→RuitkHistory` · `RUIHmr→RuitkHmr` · `RUIHost→RuitkHost` · `RUIMedia→RuitkMedia` · `RUIReconciler→RuitkReconciler` · `RUISuspense→RuitkSuspense` · `RUIVNode→RuitkVNode` |
| Q2 defaults: `ReactiveRoot`, `ReactiveRootNode`, `V`, `Hooks` — **UNCHANGED** (add `ReactiveRoot→RuitkRoot`, `ReactiveRootNode→RuitkRootNode` rows ONLY if Q2 flips) |

Surfaces the sweep reaches (verify each afterward — the counts are the census):
runtime+compiler `.gd` (incl. `guitkx.gd`'s 13 `RUIVNode` classifier spots); editor addon
lsp/view `.gd` (6); TS server (22: `declScan.ts` classifier, `virtualDoc.ts` HOOK_STUBS
ret-types + emission, `formatGuitkx.ts` `-> RUIVNode {` plain-component emission,
`server.ts`); BOTH TextMate grammars + `guitkx-schema.json` copies (7); `examples/**.guitkx`
(45 — generated `.gd` are gitignored and re-derive at build); docs-site examples (130 × 32
files); tests incl. string literals in `guitkx_test.gd`/`core_test.gd`; README/CLAUDE/
current MIGRATION docs.
**Verify:** `git grep -n "\bRUI[A-Z]"` → 0 outside `plans/archive/**` + this plan's census
tables; `git grep -c "\bRuitk"` ≈ 1806.

### Group F — contract corpus regeneration (Godot-local; NO family re-pin)

1. Fixtures were swept by Group E (87 files under `tests/contract/fixtures/`).
2. Regenerate goldens: run `contract_dump.gd` WITHOUT `--check` (writes
   `tests/contract/golden/*.json`), then WITH `--check` → 66/66 match.
3. `node scripts/corpus-hash.mjs --check` → MUST pass UNCHANGED. If it fails: STOP — a
   family-core case was touched; report, do not `--write`.

### Group G — codemod + migration guide (clean break, owner-ratified)

1. `addons/reactive_ui_toolkit/dev/migrate_0_13_0.gd` (mirror the structure of
   `dev/migrate_0_11_0.gd` + `guitkx_migrate.gd`): rewrites USER projects —
   (a) the 34-row class table (whole-word) across `.guitkx` and hand-written `.gd`;
   (b) folder paths `res://addons/reactive_ui/` → `res://addons/reactive_ui_toolkit/`
   (+ `_editor`, `_analyzer` per Q1) across `.gd`, `.guitkx`, `.tscn`, `.tres`, and
   `project.godot`'s `enabled=` list; idempotent (second run reports 0); per-file change
   counts printed; never edits inside `addons/` except the plugin list.
2. `MIGRATION-0.13.md`: one-command migration + manual notes — DELETE the old
   `addons/reactive_ui*` folders after updating (store updates never delete; duplicate
   `class_name` parse errors are the symptom of skipping this), re-enable plugins if
   disabled, rebuild `.gd` outputs.
3. CI tripwire: `tests/guitkx_rename_migrate.gd` over `examples/` (post-sweep: MUST report 0
   changes, exit 0) + a `test.yml` step beside the existing two codemod gates.

### Group H — release wave (BREAKING)

Per N21: bump both plugin.cfgs, vscode+lsp package.json (+ locks via
`npm --prefix <dir> install --package-lock-only`), vsixmanifest Version. Lane A: BREAKING
section — the full rename story, the one-command codemod, delete-old-folders note, "zero
behavior changes"; then `cp CHANGELOG.md addons/reactive_ui_toolkit/CHANGELOG.md` (NEW
mirror path — confirm the byte-identity tripwire in `guitkx_editor_test.gd` picked up the
new path from Group D's sweep). Lane B: `changelog.mjs add` shared + editor entries
(message files, UTF-8) → extract all four outputs → `extract-overview` (vscode README) →
`verify`. Discord entry (≤2000 chars, awk-gated).

### Group I — expected-leftovers audit (defines DONE)

- `git grep -c "yanivkalfa"` → 0.
- `git grep -l "ReactiveUI for Godot"` → 0. `git grep -l "Reactive UI (React for Godot)"` → 0.
- `git grep -n "\bRUI[A-Z]"` → only `plans/archive/**` (+ this plan's census tables).
- `git grep -n "addons/reactive_ui\b"` → only Tier-3 changelog bodies + `plans/archive/**`.
- `git grep -c "reactive_ui_analyzer"` → 0 (Q1=a) or 32 (Q1=b).
- `git grep -n "Reactive UI"` → only Tier-3 bodies + `<N5>`-composites.
- ANY other leftover = a missed step: report it, do not silently fix.

### Group J — full verification battery (all green before push)

1. `node ide-extensions/scripts/changelog.mjs verify`
2. Godot battery, COMPLETE SET, fresh-cache discipline (delete `.godot/`, then):
   `--editor --quit` → `guitkx_build.gd` (49/0) → `--editor --quit` → `guitkx_test.gd` ALL →
   `guitkx_editor_test.gd` (402+/0) → `core_test.gd` → `style_test.gd` →
   `router_match_test.gd` → `router_spine_test.gd` → `update_test.gd` → `demos_test.gd`
   (31/0) → `doom_game_test.gd` → `hmr_test.gd` (55) → `contract_dump.gd -- --check` (66/66)
   → `guitkx_migrate.gd` + `guitkx_modernize.gd` + `guitkx_rename_migrate.gd` (all 0) →
   `node scripts/corpus-hash.mjs --check` (UNCHANGED). Class renames touch everything — the
   usual subset is NOT sufficient.
3. lsp-server: `npm run build` + `node --test out/test/*.test.js` + `node scripts/smoke.js`.
4. Docs: `npm run build && npm run lint` in `ReactiveUIGodotDocs~`.
5. Push `rebrand/umbrella` ONLY. **[OWNER]** PR → dev → checks → merge → fast-forward master.

## 7. Phase 4 — consoles + stores **[OWNER]** (post-merge, post-Publish)

1. Publish run: ships renamed zips (N20), new listing bodies, `Proprietary` cost, new
   display names everywhere content-level.
2. AssetLib dashboards ×2: titles (N14/N15), License field → Proprietary, browse/issues/icon
   URLs. Moderator re-review possible — expected.
3. New Godot Asset Store: same updates.
4. Marketplaces: NOTHING manual (ids + display names unchanged; new versions carry the new
   content underneath).
5. Discord: post the wave entry; update pinned links (new Pages URL —
   `reactive-ui-toolkit.github.io/<N3>`).
6. Org/repo cosmetics: repo description + website field, topics, org profile README.

## 8. Phase 5 — family siblings

Godot-first (this plan). Unity/Unreal sibling plans follow the same shape (census → registry
→ groups); their identifier-rename scope was NOT decided in this wave — each sibling plan
presents its own R10-equivalent. `gdscript-analyzer`: transferred, otherwise untouched.

## 9. Aftermath + rollback

- `plans/CSHARP_LEG_PLAN.md` inherits final names when un-held (the GDScript classifier it
  references becomes `-> RuitkVNode`; its own embedded-C# grammar is Unity-shape,
  unaffected). Memory files updated by the assistant.
- `licensing-internal/BLACKOUT_STATE.md`: one-line note of the new repo URL.
- Rollback: Phase 3 = one branch (don't merge). Phase 2 transfers reversible. Post-publish:
  old releases keep old names/content by design — nothing to unwind.

## Annex A (optional, unchanged from v2) — renaming the `ReactiveUIGodotDocs~` folder
If executed, fold into Group D as Rule 6 with its reference list (publish.yml ×4, CLAUDE.md,
README ~482, test.yml grep-first, plans mentions).
